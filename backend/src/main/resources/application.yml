spring:
  datasource:
    url: jdbc:mysql://localhost:3306/${DB_NAME:reviewfullstack}?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:Venkat@2006}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQLDialect
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  sql:
    init:
      mode: always
server:
  port: 8080
jwt:
  secret: courseflow_jwt_secret_key_must_be_at_least_256_bits_long_for_hs256_courseflow_secure
  expiration: 86400000
