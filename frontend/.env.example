# Public base URL of the deployed backend, including the /api prefix.
# Set this in Netlify site settings -> Environment variables before building.
# Example: https://courseflow-api.onrender.com/api
VITE_API_BASE_URL=
